[project]
name = "contracts"
version = "0.1.0"
description = "Shared API and stream contracts"
requires-python = ">=3.14,<4.0"
dependencies = ["pydantic>2.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
